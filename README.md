# pbi

Image-aware version of `pbcopy` and `pbpaste` on macOS.

`pbi` copies stdin to the pasteboard when stdin is piped or redirected. When
stdin is a terminal, it pastes the current pasteboard content.
